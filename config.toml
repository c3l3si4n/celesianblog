# The URL the site will be built for
base_url = "https://celes.in/"


# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "hyde"
# The site title and description; used in feeds by default.
title = "Celesian"
description = "Writeups related to web security, binary exploitation and programming projects."

# The default language; used in feeds.
default_language = "en"

# When set to "true", a feed is automatically generated.
generate_feed = true

compile_sass = true

# When set to "true", the generated HTML files are minified.
minify_html = true

# Configuration of the Markdown rendering
[markdown]
# When set to "true", all code blocks are highlighted.
highlight_code = true

# The theme to use for code highlighting.
# See below for list of allowed values.
highlight_theme = "base16-ocean-dark"

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether external links are to be opened in a new tab
# If this is true, a `rel="noopener"` will always automatically be added for security reasons
external_links_target_blank = true


# Whether to set rel="noreferrer" for all external links
external_links_no_referrer = true


[extra]
# Put all your custom variables here
hyde_links = [
    {url = "https://twitter.com/c3l3si4n", name = "Twitter"},
    {url = "https://hackerone.com/celesian", name = "HackerOne"},
    {url = "https://app.hackthebox.com/profile/114435", name = "HackTheBox"},
    {url = "https://github.com/c3l3si4n", name = "Github"}
]


